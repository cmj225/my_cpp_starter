# CppStarter
C++ Project Starter

# Build
cmake -S . -B build
cmake --build build

# clang-format custom target
cmake --build build --target clang-format

# git hooks
add hook to ${PROJECT_SOURCE_DIR}/hooks}

